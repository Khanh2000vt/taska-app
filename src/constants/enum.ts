export enum THEME {
  LIGHT = 'LIGHT',
  DARK = 'DARK',
}

export enum LANGUAGE {
  EN = 'en',
  VN = 'vi',
}

export enum PHOTO {
  CAMERA,
  LIBRARY,
}

export enum EScreenSign {
  SIGN_IN,
  SIGN_UP,
}

export enum EContactForgot {
  UNDEFINED,
  SMS,
  EMAIL,
}
